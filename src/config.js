/**
 * Shared API configuration — base URL without trailing /api
 */
export const API_BASE_URL = 'https://ide2-0backend.onrender.com';

/** Full API prefix, e.g. http://localhost:5000/api */
export const API_URL = `${API_BASE_URL}/api`;
